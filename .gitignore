# OS generated files
.DS_Store
Thumbs.db
Desktop.ini

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Keep release binaries tracked (they are the point of this repo)
# But ignore very large files that shouldn't be committed accidentally
